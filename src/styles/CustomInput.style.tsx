import { StyleSheet } from 'react-native';

export const CustomInputStyles = StyleSheet.create({
  container: {
    width: '100%',
    alignItems: 'center',
    borderRadius: 5,
    borderWidth: 1,
    marginVertical: 7,
    flexDirection: 'row',
  },
  input: {
    flex: 1,
    fontSize: 14,
    fontWeight: 'bold',
    padding: 10,
  },
});
